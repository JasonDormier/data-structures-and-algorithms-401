'use strict';

const sumOfUnique = require('../sumOfUnique');

describe('', () => {

    it('should', () => {

        const test = sumOfUnique([1, 2, 3, 2]);

        expect(test).toEqual(4);

    });
});